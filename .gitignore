# secrets
.env
*.local

# deps / junk
node_modules/
.DS_Store

# supabase CLI scratch
supabase/.temp/

# Old standalone checkout of the form page. The canonical form is now
# /index.html at the repo root (merged 2026-09-17); this folder is vestigial.
web/
